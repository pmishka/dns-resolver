apiVersion: v1
kind: Service
metadata:
  name: dns-resolver
  namespace: house-infra
spec:
  selector:
    app: dns-resolver
  allocateLoadBalancerNodePorts: true
  externalTrafficPolicy: Local
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer
